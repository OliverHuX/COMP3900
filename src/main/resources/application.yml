# server port
server:
  port: 8080

# datasource
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/recipe?characterEncoding=UTF-8
    username: root
    password: seulgi
    driver-class-name: com.mysql.cj.jdbc.Driver

# mybatis
mybatis:
  mapper-locations: classpath:/mapper/*.xml
  configuration:
    log-impl: org.apache.ibatis.logging.log4j2.Log4j2Impl

## actuator
management:
  server:
    port: 8015
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    health:
      show-details: always
    shutdown:
      enabled: true
